jp:
  activerecord:
   errors:
    models:
      post:
        attributes:
          base:
            content_attachment_byte_size_is_too_big: 添付ファイルは%{max_byte_size}MB以下にしてください。[%{byte_size}/%{max_byte}]
            content_attachment_count_is_too_many: 添付ファイル数は%{max_attachment_count}つまでにしてください。
          title:
            blank: が空です。
            too_long: が%{count} 文字を超えています。
          content:
            too_long: が%{max_content_length}文字を超えています。[%{length}/%{max_content_length}]            
   attributes:
    post:
      title: タイトル
      content: コンテンツ
